apiVersion: v1
kind: Service
metadata:
  name: beispiel-nodeport
  namespace: default
spec:
  selector:
    app: beispiel
  ports:
  - name: http
    port: 80
    targetPort: 80
    nodePort: 30080
  type: NodePort
