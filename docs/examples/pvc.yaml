apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: beispiel-pvc
  namespace: default
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # storageClassName: standard  # Kommentar entfernen falls nötig
