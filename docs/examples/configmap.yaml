apiVersion: v1
kind: ConfigMap
metadata:
  name: beispiel-config
  namespace: default
data:
  APP_ENV: produktion
  LOG_LEVEL: info
  MAX_CONNECTIONS: "100"
  app.properties: |
    server.port=8080
    feature.neue-ui=false
    cache.ttl=300
